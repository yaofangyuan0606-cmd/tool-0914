# 注意：cryptography 必须锁 48.0.1。
# 更高版本（49/50）在 macOS + Python 3.13 上可能没有预编译 wheel，
# pip 会回退到 Rust 源码编译，而多数机器没有 Rust 工具链 → 直接装不上。
# 这是唯一一个必须精确锁定的包，其余可放宽。
cryptography==48.0.1
cloud-volume==12.14.4

numpy>=1.23
pillow>=9.0
requests>=2.28
tqdm>=4.64
